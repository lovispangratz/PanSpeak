general = {
    name = "Wohnzimmer Pi2B";      // so erscheint das Gerät im AirPlay-Menü
                                     // (eigener Name, falls parallel zum
                                     // Zero-2-W-Gerät im selben Netz aktiv)
    interpolation = "soxr";       // braucht die aktivierte soxr-Option
};

alsa = {
    output_device = "equal_fixed"; // explizit durch den statischen EQ (asound.conf)
    // Hardware-Lautstärke des TAS5756 nutzen (optional, bessere Qualität als
    // reine Software-Dämpfung). Kontrollnamen nach dem Boot mit
    // "amixer -c 0 scontrols" verifizieren — oft "Digital".
    mixer_control_name = "Digital";
};
